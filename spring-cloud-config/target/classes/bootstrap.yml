server:
    port: 8888

spring:
  application:
    name: configserver

  cloud:
    config:
      server:
        git:
          uri: https://github.com/miguel-dev01/spring-cloud-microservices
          username: miguel-dev01
          password: ${GITHUB_PASSWORD}
          default-label: main
